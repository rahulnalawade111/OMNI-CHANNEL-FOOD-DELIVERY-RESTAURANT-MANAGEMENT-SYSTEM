import {useEffect,useState} from 'react';import {orderService} from '../services/orderService';export default function OrderHubPage(){const [orders,setOrders]=useState([]),[error,setError]=useState('');const load=()=>orderService.list().then(setOrders).catch(e=>setError(e.message));useEffect(load,[]);const transition=async(o,status)=>{await orderService.updateStatus(o.id,status);load()};return <><header><h1>Order Hub</h1><p>Centralized, idempotent order operations.</p></header>{error&&<p>{error}</p>}<table><thead><tr><th>Order</th><th>Source</th><th>Customer</th><th>Total</th><th>Status</th><th>Action</th></tr></thead><tbody>{orders.map(o=><tr key={o.id}><td>#{o.id}<small>{o.externalOrderId}</small></td><td>{o.platform}</td><td>{o.customerName}</td><td>${o.total}</td><td><span className="badge">{o.status}</span></td><td>{o.status==='NEW'&&<button onClick={()=>transition(o,'ACCEPTED')}>Accept</button>}{o.status==='ACCEPTED'&&<button onClick={()=>transition(o,'PREPARING')}>Start prep</button>}{o.status==='PREPARING'&&<button onClick={()=>transition(o,'READY')}>Ready</button>}</td></tr>)}</tbody></table></>}
